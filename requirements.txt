# ==========================================
# 介面層 (UI/UX) - Telegram Bot
# ==========================================
python-telegram-bot>=20.0  # 提供與 Telegram API 溝通的核心套件，支援非同步 (async) 操作

# ==========================================
# 記憶體與資料層 (Memory & Data)
# ==========================================
tinydb>=4.8.0              # 輕量級 JSON 資料庫，用於儲存當下狀態 (如：是否在國外、目前位置)
chromadb>=0.4.20           # 向量資料庫，用於 RAG 架構，儲存並檢索長期個人偏好與知識

# ==========================================
# 大腦與推論層 (LLM Integration)
# ==========================================
openai>=1.0.0              # 用於串接本地端 LM Studio (其提供與 OpenAI 相容的 API 格式)
pydantic>=2.0.0            # 資料驗證神器，用來強制規範 LLM 輸出的 JSON 格式，防止意圖解析錯誤

# ==========================================
# 任務排程與自動化 (Automation)
# ==========================================
APScheduler>=3.10.0        # 處理定期自動化任務 (如：每日天氣推播、回國狀態確認)

# ==========================================
# 設定檔與環境變數管理 (Configuration & Security)
# ==========================================
python-dotenv>=1.0.0       # 讀取 .env 檔，安全管理金鑰與本地伺服器位址
PyYAML>=6.0.1              # 讀取 config/ 中的 settings.yaml 與 prompts.yaml

# ==========================================
# 系統工具與輔助 (Utilities)
# ==========================================
aiohttp>=3.9.0             # 處理非同步的 HTTP 請求 (如需呼叫外部 API 抓取天氣或轉換匯率時使用)